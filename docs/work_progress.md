# 当前工作进展

更新时间：2026-09-16

## 最新重点：完整 E2E 尚未达到要求

保留 residual gap 和未归属成本后，将已有预测的 `simulated_e2e_us` 与对应 target 的 HTTP 正式窗口实测比较：

| 已有预测面板 | 完整 E2E WAPE | P90 误差 | 最大误差 |
| --- | ---: | ---: | ---: |
| 09-12 旧 60 格 | 21.98% | 46.46% | 67.39% |
| 09-14 新 workload 12 格 | 5.50% | 14.62% | 15.19% |

这不是当前代码重新预测全部 72 格的成绩。直接使用 source 实测耗时作为 target 预测时，两组 WAPE 分别为
19.75% 和 5.07%；当前完整预测的整体误差尚未优于这个简单参照。对照仍在开启采集的环境下，不代表关闭 profiler 后的部署性能。

09-15 使用当前 C++ 库，对五份已有 profile 做了原始 DAG、同成本阶段变换和诊断性消融。确认两个问题：

- C4/W2 使用同一份实测成本，原始 DAG 为 10.482 秒，仅做 Prefill/Decode 图变换后变成 14.480 秒；
  C2/W2 同样增加约 3.528 秒。保留的 CPU 耗时/等待与新计算节点的衔接需要修复，不能只靠调整成本参数。
- C4/W2 原始 DAG 将已识别的 Prefill/Decode 设备成本全部清零后，总时间仍不变：CPU lane 的实测间隔仍撑起原耗时。
  长 scalar CPU 调用与设备执行高度重叠，但其真实 CPU 工作和阻塞等待尚未完成拆分；不能按事件名直接清零。

此外，HiCache 状态处理使用扣除 residual gap 后的 source 事件时间，完整回放却保留尚未改写的 CPU 时间；
target 计算变化对后续请求和缓存判断的反馈还需核对。以上是问题定位，不是已经完成的模型修复。

下一步先用 C4/W2 等少量样本修复同成本图变换与等待依赖，再统一请求和缓存的时间推进，最后处理剩余成本误差并回归。
不以删除所有 gap、未归属成本或使用 target 成本作为正式预测修复。
数值依据分别在 `data/modeling_runs/hicache_full_e2e_audit_20260914/summary.json` 和
`data/modeling_runs/hicache_full_e2e_root_cause_20260915/summary.json`；详细计划保留在本地
`docs/tmp/hicache_full_e2e_root_cause_and_plan_20260915.md`。这些目录按仓库现有规则不入 Git，关键结论在本节保留。

09-15 随后的实施已修复一个具体原因：CANN 显示进程与框架实际进程不同，使同一线程的长 CPU 调用和内部同步被分到不同 lane。
现在只按明确元数据和唯一线程归属统一运行时 CPU 身份，不合并设备/未知线程，也不调整成本公式。
C4/W2 同成本回放从 14.480 秒降到 10.995 秒，C2/W2 从 17.320 秒降到 14.238 秒；分别仍高于实测约 4.90% 和 3.23%。
一格 C2/W2→C4 正式预测在原参数下从 17.545 秒降到 14.469 秒，完整误差仍为 38.04%，不代表全矩阵通过。
Release/诊断构建、小型 trace 时序检查、既有 I/O 检查和 15 项 Python 测试通过；后续继续查任务线程间隔和阶段提交依赖。
主计划为本地 `docs/tmp/hicache_full_e2e_master_plan_20260915.md`，每轮记录在 `docs/tmp/hicache_full_e2e_development_log_20260915.md`。
新结果在 `data/modeling_runs/hicache_full_e2e_development_20260915/r1_summary.json`。完整 E2E 尚未完成，原历史 60/12 格成绩不追溯修改。

R2 继续修复任务队列等待：已有 enqueue/dequeue 关联时，按线程空闲与任务提交的较晚时间启动任务，
保留实测的就绪后剩余间隔，不把整个等待固定成 gap。原节点/边数量不增加，成本参数不变。
同时修正 HiCache 等待投影，避免对已由依赖表达的等待再扣一次；相关小测试先失败再通过，没有降低边界检查。

| 同一份 profile 的检查 | 原始回放秒 | R2 同成本变换秒 | 相对 HTTP 实测误差 |
| --- | ---: | ---: | ---: |
| C4/W2 | 10.481638 | 10.519666 | 0.363% |
| C2/W2 | 13.791912 | 13.864067 | 0.520% |
| C1/W3 | 9.248454 | 9.267322 | 0.275% |
| TP4 C5/W4 | 30.244560 | 30.357343 | 0.372% |

这些不是四格跨配置预测成绩；相对原图的同成本变换偏差仍都超过 0.1% 目标。旧三份 scope 与 R1 一致；
新 W4 相对 R1 之前的调查由 3.209626 秒变成 3.223095 秒，不能将两轮合并变化归因于单个修复。
正式 C2/W2→C4 已成功应用 patch，预测 14.088068 秒、target 实测 10.481627 秒，完整误差 **34.41%**；
虽较 R1 的 38.04% 改善，仍未优于直接使用 base 实测，配置收益方向也未正确。首次 patch 拒绝的结果已单独保留。
结果汇总为 `data/modeling_runs/hicache_full_e2e_development_20260915/r2_summary.json`。

四份原图清零 Prefill/Decode 设备成本后，完整耗时仍不变。下一轮核对 Gloo 通信、运行时周期查询与业务完成的关系，
以及新阶段节点应接在哪些真实提交/同步点；不能按线程名字删掉它们，也不能把余下问题全称为不可预测的随机 gap。

R3 补上消费线程的 CANN/框架身份合并，并去掉按首次出现 correlation ID 识别提交的旧启发式。
底层 CPU 叶子继承任务关联，保留设备连接；小测试证明提交时间变化能传到设备完成时间，未改成本系数。
C2 同成本回放变为 13.791912 秒，与原图一致；C4/W3 分别为 10.520885/9.268831 秒，较 R2 增加 1.219/1.509 ms，仍未达到同成本目标。
TP4 C5/W4 为 30.358314 秒，较 R2 增加 0.971 ms；四份同成本组合 scope 均不变，汇总见 `data/modeling_runs/hicache_full_e2e_development_20260915/r3_summary.json`。
正式 C2/W2→C4 预测为 13.974329 秒，完整误差约 **33.32%**，仍未优于 base 原值参照，不能宣布完成。

新的诊断已定位到周期查询线程通过事件销毁任务进入共享消费队列，并影响后续业务关键路径。
下一步核对哪些顺序是真正的资源依赖，哪些只是 source 中恰好出现的执行顺序；不按线程名删成本，也不把秒级等待硬拟合进计算或 I/O。
C2 同成本总时间一致仍可能被原始 Gloo 时间线维持，不能作为因果正确的充分证明。

R4 进一步验证了共享 CPU 队列的顺序问题。C4 两个消费线程的全部 CPU 叶子都可对应唯一提交任务；
小反例证明，计算提前后照搬 source 次序会额外等待。局部 FIFO 诊断保留所有任务，原始总时间不变，
清零计算后的主线程结束从固定次序的 9.277440 秒降至 6.521685 秒；完整时间仍是 10.481638 秒，由 Gloo 尾部维持。
这些是诊断，不是新的跨配置预测成绩；正式结果仍为上面的 33.32%。摘要分别为
`r4_queue_coverage.json`、`r4_fifo_critical_path.json`、`r4_raw_flow_inventory.json`，均在当前 E2E 开发产物目录下。
下一步先验证任务重排、补清 Gloo 与业务完成的关联，再决定简洁的正式实现。周期监测线程的源码线索和历史版本限制见本地执行日志，
不能用按名称过滤后台线程替代完整 E2E。

R5 使用已有 scheduler 外层区间核对 CPU 广播与规约的提交/完成关系，发现不能只补广播依赖：
同队列中未关联的规约仍会保留 source 的等待时间。进一步确认部分 `hicache.control.prefetch_progress.self`
包含跨线程通信等待，例如 225 µs 的 self 片段内有 178 µs Gloo 工作，并非全是 CPU 执行成本。
下一步先统一这类等待在归一化、控制成本与校准中的表达，避免重复收费；不通过调系数吸收。
本轮仅有静态分析、机制小测试和局部诊断，正式误差仍为 33.32%。证据为开发目录下
`r5_c2_collective_wait_overlap.json`，具体推进顺序见本地主计划的 R6；完整 E2E 仍未完成。

R6 进一步澄清：现有预取控制校准已经排除 `.self`，没有证据说明上述等待被拿去拟合系数。
实际缺口是跨线程变换不完整：C2→C4 中 442 个 CPU 规约提交被替换为零，对应 Gloo 工作却未变；
418 个重叠等待片段也被清零。等待拆分小测试通过，但只补依赖仍不能恢复目标配置收益，暂不并入正式路径。
下一步将提交、通信工作、等待和返回统一归属，并同时处理目标仍需要的通信；不按名字删除规约。
证据为 `r6_c2_to_c4_collective_ownership.json`，计划已推进到 R7；正式完整误差仍为 33.32%。

R7 的限定通信清理诊断只改善约 42 ms，不能解释秒级误差。R8 因此补了可选的运行时准备标记，
并重新采集一格 C2/W2：39 个请求全部成功、输出 token 全部匹配。在第一个正式请求中，两个 rank 的
2.732/2.880 秒分配空白约 99.97% 被 Triton 准备/装载区间覆盖。预热虽执行过同样的 64-token 内核，
正式请求的空闲页指针对齐不同，仍触发了另一个变体；这类空白不是纯随机噪声。

标记默认关闭，不采 snapshot、不改缓存策略，不作为额外 DAG 工作重复计时。新的原始回放为 13.964155 秒，
HTTP 实测 13.965125 秒；旧 C2→C4 正式回归与 R3 完全相同，仍为 33.32% 误差。
下一步沿现有分配器状态检查：HiCache 变化怎样改变准备的发生条件，哪些代码已在正式窗口前准备好；
不能直接扣掉 2.7 秒或用 target 残差拟合。证据见开发目录的 `r8_base_preparation_summary.json`、
`r8_base_gap_matches.json`，本地主计划已推进到 R9；本次为诊断采集，不是新的完整 E2E 精度成绩。

R9 已将分配器账本独立为运行时组件，保留容量算法，并追踪空闲页索引数组的切片起点：分配推进起点，
非空释放或合并重建数组时归零，仅靠容量对账无法确定时标为未知。目标分配记录按 batch 保存，包含预热
和正式请求；它与逐请求 Prefill 计算记录分开，目前不改变 duration，也尚未用于预测编译/装载成本。
首轮状态诊断中，C2 前六个正式请求的起点为 17，C4 为 0；这还不足以证明目标需要准备几次，
仍需验证目标自己的预热过程。新 base 的完整 self patch 未通过资源依赖检查（`lane_dependencies_exact=0`），
关闭 patch 的状态诊断不能算作 self 通过；该失败尚未解决，不能据此宣布完整 E2E 修复。
提交前复验通过 5 项探针测试、15 项 Python I/O 测试及 C++ I/O、trace 时序检查。

R9 随后修复了预热工作量的一个边界错误：extend 重新查完整路径时，曾越过请求的前缀查询上限，
把部分仍需计算的重复输入误算成零 token。现在保留查询上限、按目标页大小计算可复用范围，成本参数不变。
两 rank 共 8 个分配器准备事件已与 base 实测的请求和参数逐项对应；C4 目标序列推导为正式阶段无新增准备，
这项目标判断尚未进行带标记实测验证。准备耗时仍未进入 DAG 变换，旧 cross 完整误差仍为 33.32%。
self 失败已进一步定位：原样保留 I/O 的 NoOp 决策没有新端点，但资源后置检查仍要求它们存在。
下一轮要统一这两种检查语义，同时保留对真正缺失依赖的拒绝；当前未绕过检查，也未把 self 算作通过。

R10 已修复这一检查矛盾，且反例确认真实改写缺端点/缺边、NoOp 实际修改等情况仍会失败。
新 base self 的 448 项及相关拓扑检查通过，完整模拟 13.964155 秒、HTTP 13.965125 秒；旧 cross 仍为 33.32% 误差。
进一步诊断表明，单独拆出有标记的准备间隔不能充分改变总时间；结合局部队列与通信依赖后，主线程能提前约 2.80 秒，
但全图仍被周期查询线程的 source 时间线限制。主线程 11.358412 秒、全图 12.994839 秒均是诊断值，不是正式 HTTP 精度。
下一步核对真实响应出口和周期任务的完成依赖，不能按线程名删掉尾部，也不能把主线程结束当作最终成绩。

R11 已加入可选的终止请求响应边界探针，覆盖 scheduler 发送、tokenizer 分发、JSON 序列化和最后一次 ASGI body send。
默认关闭，只记录请求 ID 与时间；ASGI 发送返回不代表客户端已经收到响应，完整 E2E 仍以 bench 为准。
9 项探针测试及实际 SGLang 类的安装、响应消息透传检查通过；带新标记的真实模型采集和响应依赖分析尚未完成。
本次提交前再次通过 9 项探针测试、15 项 Python I/O 测试及 C++ I/O、trace 时序检查，未重跑完整预测矩阵。

R11 随后完成一个 C2/W2 真实补采：39 个请求及 token 检查全部通过，四类响应标记逐请求对齐。
14 个正式请求从调度器发送返回到客户端收到只需 2.411～3.125 ms，合计 38.529 ms，不是此前秒级偏差的主因。
原始 DAG 14.112257 秒、HTTP 14.112980 秒；这只是 source 回放，不是 cross 精度改善。
发送本身位于现有 CPU gap 内，正式接入需拆分原 gap，避免重复收费；客户端请求间合计 40.850 ms 也必须保留。
后台资源依赖与响应终点的小测试通过，但实际响应接图、串行请求到达和目标等待变化尚未完成；正式 cost 和终点本轮未改。

R12 已把观测到的发送区间拆成零耗时连接点，14 个正式请求全部绑定，743961 个原节点的开始/完成时间逐一验证无变化。
完整、control-only 和原组合指标均守恒，旧 cross 仍为 33.32% 误差。沿新连接点补上 base 实测响应尾部的诊断仍提前约 28.34 ms，
说明原始全图总时间接近并不保证业务路径内部正确。主要提前跳变发生在流同步处，且发现绑定的设备前驱原始时间晚于 CPU 返回的反例；
下一步核对同步前驱与时钟，不以固定补偿掩盖。正式客户端终点与完整预测仍未完成。
本次分层提交前复跑 validation/Release 增量构建、C++ I/O 与 trace 时序检查、9 项探针及 15 项 Python I/O 测试，均通过；未重跑完整矩阵。

R13 在不加载模型的 NPU 小测试中复现了设备时钟漂移：同一采集内相隔 40/80 秒的计算，原始 trace 的结束时间分别晚于 CPU 同步返回约 0.454/1.029 ms。
独立函数探针确认 CPU 区间与返回正常。仅用两次小测试的物理对时点换算设备时间后，矛盾消失；未参与速率计算的 C2/W2 78 个邻近同步候选也全部恢复正时间差。
这项结果定位的是采集时间坐标，不是新增 cost 拟合；仅验证本机 device 2，尚未接入正式预测，也未证明完整业务路径漂移已修复。
下一步核对各设备首尾对时来源，统一验证设备时间换算，再回到同步等待与 HTTP 请求链；正式 cross 结果不变。

R14 进一步用设备 2、3 的驱动计数器做独立对时。全图诊断发现，设备执行视图与通信视图必须一起换算；
仅调整执行视图会破坏同一事件的合并。两类视图一起处理后，476 个同步候选中原有的 323 个负返回时间差消失，
但业务响应路径仍提前约 44.9 ms，说明时钟不是唯一问题。这些时间换算仍是本地诊断，尚未接入正式采集或预测。

本轮产品修复限定在 DAG 因果关系：CPU 提交必须先于其关联的设备执行，不能由两套时钟的显示先后决定方向；
事件记录与等待从唯一的 CPU 记录取关联信息，不再取时间排序第一项。同时统一读取原始及后加参数，避免设备节点被误认成 CPU。
提交前 validation/Release 构建、C++ I/O 和 trace 时序检查、9 项探针与 15 项 Python I/O 测试均通过。
两个正式入口回归也通过，各 448 项 patch 检查就绪：新 base self 仍为 14.112257 秒，旧 C2/W2→C4 仍为 13.974329 秒。
后者对 target HTTP 10.481627 秒的误差仍约 33.32%；本次修复未宣称提高完整 E2E 精度，未重跑完整矩阵。
下一步仍需验证时钟换算与新依赖规则的组合，再推进真实 HTTP 请求链；本次按核心修复、测试、文档分层提交，诊断数据及临时计划保留在本地。

R15 已找到响应路径漏计的一处原因：114 个设备 EVENT_WAIT 没有接到所等待的记录，却仍被缩成 10 µs。
现在根据主机调用顺序选择 Event 记录，后续复用不改变已提交的等待；缺失或重叠调用无法证明顺序时保留原始耗时。
真实回归还暴露了同微秒设备顺序颠倒，已使用原始纳秒余量修复，并删除人工移动 WAIT 时间戳的处理。
CPU 合成 self 片段继续按已有整微秒区间顺序处理，避免把不同精度的数据混排；两次失败及原因保留在本地日志。

最终 source 诊断中，14626 个设备等待全部接到前驱；时钟对齐后的最后响应偏差从提前 44.865 ms 缩小到晚 9.662 ms。
这不是新的 cross 成绩，也没有增加补偿系数。公共入口的 self / 旧 C2/W2→C4 均通过 448 项 patch 检查，
完整预测仍为 14.112257 / 13.974329 秒，旧 cross 误差仍约 33.32%。构建、C++ 检查及 24 项 Python 测试通过。
接下来将 source 的 HTTP 请求观测正式接入 manifest 和请求链，再组合准备、队列与通信变化；完整 HTTP 终点、target 状态时钟及最终矩阵尚未完成。

R16 已增加可选的请求接收、分发就绪观测，并让 profile manifest 显式声明 workload report。
建模入口优先使用声明的报告，保留旧 manifest 的发现方式；多份报告要求明确选择。逐请求 HTTP 链尚未接入 DAG。
本次整理时重新运行的 29 项 Python 测试、C++ HiCache I/O 与 trace timing 检查均通过，validation / Release 构建通过。
首轮 C2/W2 补采在服务就绪前退出（code -9），没有开始 workload，也没有产生有效请求报告；原因尚未确认，不能据此判断探针或模型精度。
失败资产保留在 `data/profile_runs/sglang/20260915_155942_e2e_request_boundaries_C2_W2`，不计为有效采集，不更新现有精度结果。

原配置重试 `data/profile_runs/sglang/20260915_161042_e2e_request_boundaries_C2_W2` 已完成，约 3.3 GB，39 个请求 token 回放一致。
全部请求的接收、两个 rank 分发、发送及客户端结束顺序通过检查，正式 14 个请求的 HTTP 窗口为 13.946083 秒。
此前启动失败的首因仍未确定；重试没有改变配置、超时或模型参数，不能把成功归因于某项未做的修复。

正式建图已接入 42 个请求接收/分发零耗时点；逐 rank 检查 743206 个原始执行节点，完成时间变化全部为 0，完整和 control 回放保持不变。
此处未 patch 原图的 scope 未标记，scope=0 不是组件精度证据。公共 self / 旧 C2/W2→C4 则分别通过 448 项 patch 检查。
新 self 使用已有 C2 参数做接线回归，输出 13.946120 秒；虽然接近 HTTP 实测，当前终点仍不是显式客户端响应，不能据此宣称完整 E2E 达标。
旧 cross 仍为 13.974329 秒、误差约 33.32%。下一步连接真实客户端链并核对 Gloo 广播/空轮询等待，而不是给 gap 加总量补偿。

R17 用同配置补齐 tokenizer 提交和各 rank 接收区间，资产位于 `data/profile_runs/sglang/20260915_163359_e2e_request_delivery_C2_W2`，约 3.3 GB。
39 个请求身份、token 和收发顺序均通过检查，正式 HTTP 为 13.846886 秒。正式请求中两 rank 均在 tokenizer 开始提交之后才进入本次接收，
没有证据支持删除一段“提交前已经等待很久”的固定时间。每次接收区间内都看到长度/正文两次 CPU 广播及其 Gloo 工作，但原始全局序号均不可用。

正式建图已加入接收开始点，共 70 个请求相关点全部绑定；744542 个原始执行节点完成时间变化为 0。公共 self 与旧 cross 各通过 448 项 patch 检查，
self 为 13.846680 秒，旧 cross 仍为 13.974329 秒。前者仍不是显式 HTTP 客户端终点通过的证明。31 项 Python 测试及 C++ 构建、检查通过。
已有 allocator 准备次数推导也复验：C2 正式窗口需两 rank 各准备一次，预测 C4 全部已准备；还没有把这一差异作为正确的 DAG 时间变换接入。

R18 已在正式 C++ 工作流中接入串行客户端链，只读取 source manifest 声明的请求报告与探针。
客户端完成时间单独输出，不删除后台工作，也不清除 source residual 等待。小图验证计算变快/变慢会传递到后续请求，
并验证后台完成时间不被误当作 HTTP 出口；不支持的请求窗口明确报告不可用。

三次公共回放产物位于 `data/modeling_runs/hicache_full_e2e_development_20260915`：

- `r18_base_self`：显式 HTTP 13.806076 秒，source 实测 13.846886 秒，相差约 40.810 ms（0.295%）；图结束时间仍为 13.846680 秒。
- `r18_new_cross`：用 R17 的 C2 source 和已有 C4 模型参数，显式 HTTP 为 14.058474 秒，图结束时间为 14.066278 秒。
- `r18_old_cross`：旧 source 缺少已声明的请求报告，HTTP 结果明确不可用；旧图预测保持 13.974329 秒，没有静默替代 HTTP。

三次各通过 448 项 patch 检查。31 项 Python 测试、C++ validation/Release 增量构建、I/O 与 trace timing 检查再次通过。
这次完成的是客户端依赖和出口的接入，不是完整精度验收；新 cross 换过 source，不与旧 cross 冒充同条件精度改进。
首请求时间原点、跨 rank 等待及 allocator 准备成本变换仍待处理，现有组件评分也尚未切换为 HTTP 评分。

R19 沿显式 HTTP 路径找到并删除了一条无依据的全流屏障：旧核心把缓存查询后最近的任意 CPU 事件当成请求边界，
有时选中后台 `aclrtQueryEventStatus`，再把其周期性等待传给设备复制。小反例复现了无关后台任务变慢时，
请求被错误地从 121 µs 推迟到 1011 µs；删除规则后请求耗时不再受其影响，真实提交依赖仍有效。

三次正式回归 `r19_base_self` / `r19_new_cross` / `r19_old_cross` 各通过 448 项 patch 检查；
显式 HTTP 和完整图时间均与 R18 相同，新 self 的排除 gap 范围减少 91 µs，其余所查 control/scope 不变。
不能据此宣称正式 cross 精度已有提升：准备成本和多生产者队列的目标时序还未进入正式预测。

受限诊断给出了依赖修复的实际意义：在 R17 source、相同 C4 工作/成本下，仅拆出已标记的准备 gap，HTTP 为 13.578124 秒；
再按任务到达顺序调度 CPU 队列，修复前为 13.316462 秒，删假屏障后为 11.251199 秒。
后台节点和其它 gap 均保留，完整图结束仍为 13.788567 秒，不能与客户端完成混为一谈。
这些是机制敏感性结果，不是已上线 predictor 或完整矩阵成绩。两 rank 的已知准备覆盖分别为 2.740143 / 2.860078 秒，不能相加作收益。
产物在同一开发目录的 `r19_after_boundary_fix.json`；源码、反例与下一块实施顺序见临时主计划和执行日志。

R20 已将上述 CPU 任务队列调度接入正式模拟器，不再只存在于诊断脚本。小图测试覆盖任务到达顺序改变、
队列忙闲、同时到达、多叶子任务、重复回放、身份缺失及队列依赖冲突。提交返回晚于 worker 开始的 20 处观测
均只有 1～5 µs，总计 48 µs；实现明确使用提交返回作为保守到达时间，而不把差值藏进负 gap。

已有三次正式回归 `r20_base_self` / `r20_new_cross` / `r20_old_cross` 的 HTTP、完整图及所查组件时间与 R19 一致。
新 self / cross 的 HTTP 分别为 13.806076 / 14.058474 秒；旧 cross 缺少唯一 workload report，仍只报告完整图
13.974329 秒，不冒充 HTTP。准备成本尚未改动，因此这次接入本身没有带来正式预测精度提升。
采用正式队列模拟器的准备敏感性诊断复现了 HTTP 从 14.058474 秒到 11.251199 秒的变化，
完整图从 14.066278 秒到 13.788567 秒；只改动两处已观测的准备覆盖，其他 gap 和后台节点保留。
两次诊断的最大队列深度为 119 / 123。产物为 `r20_formal_queue_preparation_sensitivity.json`，仍不是正式成本预测成绩。

提交前重新检查了 validation / Release 构建、C++ I/O 与时序测试，以及 31 项 Python 测试，均通过。
下一步是把目标预热和正式分配序列推导出的准备需求，与 source 中实际承担准备成本的位置连接起来；
未知准备变体的成本来源、其余 CPU 等待、正常运行对照和完整矩阵验收仍未完成。

R21 已把有 source 观测依据的 allocator 准备变化接入正式 patch，不再依赖外部准备消融脚本。
准备需求由目标预热和正式分配序列推导，计时来自 source prepare/load 区间并集，没有新增拟合系数。
`r21_base_self` 保留全部准备成本，HTTP 仍为 13.806076 秒；`r21_new_cross` 只变换两个已知准备 gap，
HTTP 从同 source 的 R20 14.058474 秒降至 11.251199 秒，后台完整图为 13.788567 秒，组合 scope 仍为 7.549606 秒。
两 rank 覆盖共 5.600221 秒不是 HTTP 收益，实际预测变化为 2.807275 秒。

三次预测完成后，读取旧 C4/W2 workload report 的 HTTP 10.481626709 秒作历史参照：
同一新 source 的 R20/R21 绝对相对误差从 34.125% 降至 7.342%，R21 仍高估约 0.770 秒。
这不是新采集的一组配对实验，也不是完整矩阵 WAPE；不能与使用旧 source 的 33.32% 混为同条件比较。
`r21_old_cross` 因缺少准备观测而明确报告不可用，不修改其原成本，完整图保持 13.974329 秒。
三次各通过 448 项 patch 检查；validation/Release 构建、C++ 机制测试及 31 项 Python 测试通过。
准备的新变体成本与位置、剩余等待、状态时钟、正常运行和最终矩阵仍须继续，完整 E2E 尚未达标。

R22 逐请求定位后修复了一个 HiCache 等待条件错误：原实现把移除 source 轮询等待与构造 target 完成节点绑在一起，
目标立即返回时反而保留了旧等待。现在两者独立判断，只处理 source 已有证据的等待覆盖，不新增拟合系数。
小反例先失败再通过；self 与未知 gap 的保留规则不变。
`r22_wait_cross` HTTP 为 **10.841831 秒**，相对同 source 的 R21 减少 0.409368 秒，
对历史 target 10.481626709 秒的误差从 7.342% 降至 **3.437%**，仍高估约 0.360 秒。
减少量全部来自前六个 survival 请求，后八个请求保持不变；后台完整图为 13.453933 秒，组合 scope 仍为 7.549606 秒。
`r22_wait_self` HTTP 保持 13.806076 秒；旧 source cross 完整图降至 13.554725 秒，但仍缺正式 HTTP 观测和准备成本覆盖。
三次各通过 448 项 patch 检查，validation/Release 及机制测试通过。该单格结果不是完整矩阵 WAPE，也不是正常运行验收。

同时修复 source 阶段观测摘要的时机：以前在 patch 后重新读原节点，会把已迁走的设备成本报告为 0；
现在变换前记录，两次回归各 196 项 source 成本与模型输入对应检查通过，预测时间不因此改变。
下一步继续定位前六请求剩余的 CPU/跨 rank 等待与阶段重叠，并处理新准备变体的成本覆盖；主计划 A–E 未完成。

R23 又定位并修复了 false 预取检查的调用边界问题：Python fact 不包含结束后的字段提取/输出，
按它移除 CPU 叶子会留下外层控制标记中的片段。现在有完整同线程控制标记时使用整次已取消调用的归属；
保留终止检查、调用外工作和未知时间，成本参数不变。
`r23_cross` HTTP 为 **10.732311 秒**，对同一历史 target 10.481626709 秒的误差为 **2.392%**，仍高估约 0.251 秒。
相对 R22 减少的 0.109520 秒均来自前六请求，后八请求不变；组合 scope 仍为 7.549606 秒，后台图仍为 13.453933 秒。
`r23_self` HTTP 仍为 13.806076 秒、后台图和 control 不变，但归属修正使其组合 scope 从 6.728276 变为 6.837491 秒；
这是保留的检查工作被重新归入 HiCache 范围，并非完整耗时发生变化。
旧 source cross 后台图为 13.437626 秒，仍缺 HTTP 和准备观测，不能当作完整预测成绩。
三次各通过 448 项 patch 检查；HiCache 模型结果、source phase 观测及准备计划与 R22 完全一致，
validation/Release 构建、C++ 机制测试和 31 项 Python 测试通过。

同时确认阶段聚合仍不能通过一般同成本检验：两层计算/通信交替的小图，原始为 39 µs，现有变换为 44 µs。
代表 cross 的单格误差低于 3% 不等于结构或完整矩阵达标。下一块优先处理阶段提交/同步切点，
继续核对首请求的通信等待；新准备变体、状态时钟、正常运行及最终矩阵仍未完成。

R24 已用保留原算子提交/同步的阶段变换替换五节点聚合。两层反例恢复为 39→39 µs；
计算变慢、末次提交推迟、CPU 提前准备和缺失结构模板的测试通过。
新 C2 source 的同成本检查覆盖 744682 个原节点，C4 输入覆盖 707045 个原节点，两者完成时刻均逐节点完全不变。
这比只比较总时间更强，但仍不代表不同算子路径的结构泛化已完成。

正式 `r24_cross` HTTP 为 **10.746759 秒**，对历史 target 的误差为 **2.529%**，高估 0.265132 秒；
相对 R23 净误差增加 14.448 ms，不能称为精度继续提升。前六请求累计高估从 284.113 降为 251.229 ms，
后八请求从低估 40.632 ms 变为高估 6.700 ms；原来的部分正负抵消不再存在。
`r24_self` HTTP 为 13.853408 秒，与纯 source 同成本回放 13.856586 秒不同，因为 self 使用成本模型的预测值。
两次正式回归各通过 448 项 patch 检查，模型结果、source phase 观测和准备计划与 R23 完全相等；没有调整系数。
组合 scope 为 self 6.854756 秒 / cross 7.573496 秒。control 指标因旧 synthetic 计算不再绕过区间排除而明显下降，
不是完整 HTTP 收益，不能沿用旧值作纯 CPU 成本对照。

恢复算子切点后还消除了新增边校验中的二次日志扫描，保留唯一性、effect 和实际边检查。
本次分层提交前复验通过 validation / Release 增量构建、C++ I/O 与时序检查及 31 项 Python 测试；
未重跑完整矩阵。预取等待修复、阶段时序修复、校验性能优化、测试和文档分别提交，实验数据与临时计划保留在本地。
下一步扩展同成本与关键 cross 覆盖，检查剩余 CPU/通信等待及算子形状变化；
新准备变体、统一状态时钟、正常运行和最终 60 / 12 格验收仍未完成。

R25 将阶段同成本检查扩到 C1/W3 与 TP4 C5/W4，分别 646900 / 5079878 个原节点时刻全部不变。
旧 W4 缺少 HTTP 链和准备观测，因此重新采集同一 TP4 C5/W4 base，沿用原配置、绑定及 token 计划，只启用已有轻量 timing 观测。
新资产为 `data/profile_runs/sglang/20260915_194735_e2e_request_delivery_C5_W4_TP4`，需保留。
32 个请求顺序和输入/输出 token 检查通过；新原图与同成本 HTTP 均为 **30.343101 秒**，比实测高约 0.314 ms，
5078278 个原节点时刻全部不变。没有新增 target 校准或重拟合。

使用新 base、原有同 base 参数预测 G1，完整 HTTP 为 **30.164832 秒**；历史 target 实测 **26.243120 秒**，
高估 **3.921712 秒，APE 14.943771%**。912 项 patch 检查通过，但准备覆盖为 partial：四个 rank 各有一个目标新变体成本未覆盖。
目前仍保留 source 在第 4/6 个正式请求中的准备，却未补入目标分配历史推导出的第 2 个请求的准备；对应请求分别明显高估/低估。
这些位置与误差相吻合，不等于已经用目标区间证明全部根因。结果来自 `r25_new_w4_cross` / `r25_new_w4_request_score.json`，
不是去 gap 后的成绩，也不是新配对测量；其余请求仍有百毫秒偏差。

另一个小反例确认 Decode 成本落点错误：公式只改变注意力成本，DAG 却把变化分摊给全部 Decode kernel，
带中间同步的小图因此由应有 64 µs 变成 74 µs。下一步分别处理新准备的成本/位置和 Decode 成本归属，
不根据上述误差调整 I/O 或计算系数。完整状态时钟、正常运行和最终矩阵验收仍未完成。

R26 已修复 Decode 内部的成本分配：注意力变化只落在注意力算子，非注意力保持原公式的成本；
真实成本回放同步提供注意力分量。64→74 µs 的小反例恢复为 64 µs，并覆盖分量增减、缺失模板和非法 oracle 输入。
C2 的 744682 个原节点同成本时刻仍全部不变；196 项预测成本经 oracle 接口原样读回后，除审计字段外整份摘要与正式预测完全一致。
正式回归中，W4→G1 HTTP 仍为 30.164832 秒、误差 14.943771%；C2→C4 为 10.746764 秒、误差 2.529543%，仅增加 5 µs。
两项的模型结果、source phase 观测和准备计划与修复前一致，没有改变成本参数。细节修正不掩盖新准备仍未覆盖的主要问题。

本次分层提交前复验通过 validation / Release 增量构建、C++ I/O 与时序检查，以及 32 项 Python 测试；
未重跑完整矩阵。提交按 C++ 成本分配、Python 回放接口、回归测试和文档拆分；实验资产及临时计划继续保留在本地。

R27 为准备成本补齐了路径证据和提交位置。可选 timing 探针现在能区分实际 IR 编译、磁盘复用与异步提交，
不按耗时猜路径；默认仍关闭，没有新增 snapshot 或逐次 launch 记录。实际 Triton 的无设备测试和 34 项 Python 测试通过。
旧 C5/W4 图中 48 个正式分配调用均找到唯一的主线程 enqueue 及其 worker/device 关联，8 次已有准备完整落在相应提交前 gap 中。

已完成同配置 C5/W4 的一次补采，资产为 `data/profile_runs/sglang/20260915_204815_e2e_preparation_paths_C5_W4_TP4`，需保留。
4 份 Torch、4 份 native 和 5 份 Python trace 齐全，manifest ready；32 个请求顺序及输入/输出 token 检查通过。
32 次 allocator prepare 均明确为同步编译，中位 2.744774 秒；4 次首次装载中位约 20.289 秒，28 次后续装载中位约 2.144 ms。
正式 HTTP 实测为 30.626298 秒，与上次 30.342787 秒的差异尚不能归因于探针开销；没有做配对重复试验。
这是成本来源证据，不是跨配置精度提升。R27 结束时，新准备的正式成本接入与回归尚未完成，cross 成绩仍为 R26。
新 source 的 5082871 个原节点在同成本阶段变换后时刻全部不变；原/变换 HTTP 均为 30.626463 秒，
比实测高约 0.165 ms。新资产已完成采集和原图检查，不需要为获取这批准备路径标签再次采集。

R28 已接入新增 allocator 准备成本：只使用所选 base、同 rank 的同步编译与后续装载中位数，
首次装载单独处理；按变体与批次判断缓存复用，将成本放在真实的主线程提交前。
source 准备删除和 target 新增一起生效；样本、提交位置或运行时条件不足时全部撤回，不清除剩余 gap。
当前只支持已观测条件下的同步并行首用近似，异步编译、正式窗口首次运行时初始化等仍未覆盖。

本轮关键回归使用完整 HTTP 正式窗口，未排除 gap 或未归属成本：

| 预测 | 预测 / 实测（秒） | 绝对相对误差 |
| --- | --- | --- |
| 新 C5/W4 自回放 | 30.626716 / 30.626298 | 约 0.0014% |
| 新 C5/W4 → G1 | 27.825813 / 26.243120 | 6.030887% |
| C2/W2 → C4 | 10.746764 / 10.481627 | 2.529543% |

结果保存在 `data/modeling_runs/hicache_full_e2e_development_20260915/r28_{w4_self,w4_cross,c2_cross}/run_summary.json`。
相同新 source、相同工作量和成本参数的诊断对照中，不启用新增准备估计时 HTTP 为 30.457576 秒，
启用后为 27.825813 秒，相差 2.631763 秒。这是准备变换的同源对照，不是跨 rank 成本之和，也不是额外的正式精度成绩。
不能将相对 R26 的全部改善归因于代码，因为 W4 使用了重新采集的 base。C2 的原预测结果保持不变。
W4 仍高估约 1.583 秒，未达到 3% 目标；尚未完成全矩阵及非 profiling 运行验证。

本次分层提交前重新通过 validation / Release 增量构建、C++ I/O/phase/preparation 与时序检查，以及 34 项 Python 测试。
未重跑完整矩阵；上述关键 cell 结果来自 R28 已完成回归。之后增加的异步观测保护已通过机制测试，未另跑真实 cell。
提交按采集实现、建模实现、回归测试和文档拆分；采集数据、诊断结果及临时计划/日志继续留在本地，不纳入 Git。

R29 核对了 W4 的 CPU 广播：通信按树式分支完成，不是所有 rank 到齐后才能统一返回。
局部依赖诊断在原始 source 上使 HTTP 增加 208 µs，但尚未接入正式预测。
cross 诊断发现 212 个广播等待已由预取轮询变换处理，因而没有应用；不能把它们再作为独立等待重复计费。
后续需要区分 target 仍需执行的通信和随 source 轮询消失的通信，不能仅补一批广播边就宣布完成。

R30 删除了另一项没有因果依据的规则：不再把 HiCache 等待区间投影到其他线程，仅凭时间重叠扣除它们的 gap。
小测试确认，缩短前台等待不会缩短独立线程的计时，也不会重复扣除任务队列等待；已确认归属的 HiCache 等待仍正常变换。
这项修复与上述 212 个已归属的轮询等待是不同问题，没有调整 I/O 或计算成本参数。
两格正式回归均完成：C2/W2→C4 为 **10.746764 秒、误差 2.529543%**，C5/W4→G1 为 **27.825813 秒、误差 6.030887%**，
均与 R28 完全一致。结果位于当前 E2E 开发目录的 `r30_c2_cross`、`r30_w4_cross` 及对应 `r30_*_request_score.json`。
validation / Release 增量构建、C++ I/O 与时序检查、34 项 Python 测试通过；未重跑全矩阵或正常运行验证。
本次按核心修复、测试、文档分层整理，诊断和实验资产仍保留在本地。完整 E2E 目标尚未完成。

R31 的调度分析进一步确认：53 轮预取等待中，50 轮空闲，最后 3 轮已经启动计算，不能整轮删除。
只对其余 556 个局部广播调用做依赖诊断，保留已有 HiCache 等待变换，W4→G1 的完整 HTTP 从 27.825813 降为 27.597450 秒，
诊断误差为 **5.160705%**。这不是正式模型成绩：212 个已归属调用的 target 轮询、CPU 规约与工作池尚未完成建模。
几个主要高估请求几乎没有改善，余下约 1.354 秒偏差不能全归于广播。

R32 在现有 timing/full 开关下补充 CPU Gloo 通信元信息，包括组、成员、前后序号与同步/异步模式；默认 off 不安装。
不读 tensor 内容、不加同步；建图只保留观测，不增加执行成本。38 项 Python 测试、ruff、C++ 构建及检查通过，
实际 SGLang 镜像的两 rank CPU 小测试验证了跨 rank 身份、多个进程组和异步返回行为。
尚未重新采集真实模型或测定探针开销，也没有把局部广播诊断迁为正式预测。下一步同时核对 CPU 提交链与通信归属，
不以统一倍率拟合剩余 gap；完整状态时钟、正常运行与最终矩阵验收仍待完成。
证据位于当前 E2E 开发目录的 `r31_scheduler_rounds.json`、`r31_w4_retained_broadcast_tree.json`、
`r31_w4_retained_broadcast_score.json` 和 `r32_cpu_collective_probe_check.json`。

R33 发现一个尚未修复的结构缺口：source 发生回载后，HiCache consumer 状态会沿用到 Decode，
每层读取 K/V 仍会提交等待事件，即使回载已经完成；target 不需要回载时，这些调用不存在。
W4→G1 的 formal_0/7/10 每 rank 各少 1024 次调用，对应 64 层 × 8 次 Decode × K/V 两次读取。
现有 source-only 回载决策推导出的调用数变化，与 12 请求 × 4 rank 的实测变化全部一致。
这只验证了调用数量的机制，不能把调用周围的 gap 全部删掉，也不代表等待成本变换已完成。
证据为当前开发目录的 `r33_base_phase_cpu.json`、`r33_target_phase_cpu.json`、
`r33_load_wait_count_comparison.json`；target 数据只用于核对，不进入预测参数。

R34 为后续归属分析补上可选的逐层等待探针：batch 内缓存起止时间，结束后一次写出；默认关闭，
不采 snapshot、不增加同步。建图仅保存观测，尚未修改等待节点或 cost model。
本次整理补齐探针的批量写出、完整列表、异常透传、作用域和关闭路径测试；
真实模型补采、探针开销测量，以及等待调用增删的 DAG 变换仍待完成，正式 E2E 成绩未更新。
本次分层提交前通过 44 项 Python 测试、ruff、validation / Release 增量构建，以及 C++ I/O 与时序检查。
提交按采集实现、DAG 观测保留、回归测试和文档拆分；未运行完整预测矩阵，数据及临时分析资产保留在本地。

R34 已完成一次同配置 C5/W4/TP4 base 补采，目录为
`data/profile_runs/sglang/20260915_225731_e2e_layer_waits_C5_W4_TP4`。32 请求的输入输出 token 全部匹配，
4 Torch / 4 native / 5 Python trace 齐全，正式 HTTP 为 31.599269 秒。
432 个正式 batch 包含 41472 次逐层等待；全部找到唯一主线程 enqueue 和关联的 CPU worker 调用。
41016 次完整落在现有叶子/gap 边界内，456 次切到其他叶子的边界，尚不能直接作为完整删除计划。
每 rank 的调用包络合计约 1.95～1.98 秒，不能跨 rank 相加当成 E2E；设备等待关系也需继续核对。

R35 修复了删除任务后的队列残余延迟重算：原来删除中间任务后，下一任务会把被删执行时间重新算成等待。
三任务反例修复后由 34 降到应有的 23 µs，首/尾任务删除、重复模拟和提交推迟检查均通过。
新 base 的原图与同成本回放 HTTP 均为 31.599557 秒，5080637 个原节点时刻全部不变，
相对 HTTP 实测仅差约 0.288 ms；这证明自回放守恒，不是 cross 精度结果。

R36 同时发现这次补采的通信探针存在嵌套重复记录，不能作为已经全部通过的通信身份资产。
已补调用上下文去重；46 项 Python 测试、ruff、C++ 检查与两种构建通过，
实际镜像在加载 NPU 兼容包装后，两 rank CPU Gloo 的三次通信均只记录一次、身份匹配、异步返回不变。
未再次采集模型，也未用诊断区间直接扣减预测耗时。新旧 base 约 0.973 秒差异包含尚未拆清的采集影响与波动。
下一步处理逐层调用的边界歧义、worker/设备依赖及新增方向模板，再做关键 cross；正式 R30 成绩不改写。
证据为开发目录 `r34_capture_summary.json`、`r34_layer_wait_ownership.json`、`r35_source_same_cost.json`、
`r36_cpu_collective_compatibility_check.json`。

本次分层提交前再次通过 46 项 Python 测试、ruff、validation / Release 增量构建，以及 C++ I/O 与时序检查。
提交按 CPU 队列修复、通信探针去重、回归测试和文档拆分；未重跑预测矩阵，实验数据及临时计划/日志继续保留在本地。

R37 确认 Python 墙钟不能直接作为 Torch 叶子的删除边界：单卡 API 小测试中，128 次调用有 53 次
墙钟边界切到后续操作，使用 Torch 本次采集的计数器换算后为 0。现已将原始计数器→每 rank 的 manifest 换算→
C++ 纳秒边界接入；50 项 Python 测试、C++ 检查和两种构建通过，实际镜像端到端读入的 128 个区间均无交叉。
旧 profile 不追溯修补；没有重采模型，也没有新 cross 成绩。证据为 `r37_wait_clock_retry/summary.json` 和 `r37_clock_loader_check.json`。

同一 API 测试还确认，已完成事件的 128 次 CPU wait 可以完全没有设备 EVENT_WAIT 记录。
当前图只为显式设备 WAIT 连接 Record，遗漏了这种潜在依赖：小图将复制变慢后，事件 102 µs 才就绪，
消费端仍在 27 µs 完成（`r37_missing_device_wait.json`）。下一块按原生 Event/Stream 和提交顺序补齐逻辑等待，
再继续 HiCache 逐层调用的双向增删；不能用一个成本倍率掩盖缺边。

R38 已补齐无设备 WAIT 记录时的逻辑依赖。零成本等待点按原生 Event/Stream 与 host 调用顺序连接，
不把异步 CPU 返回改成阻塞，也不延后前序设备工作。小图中复制变慢后消费端由错误的 27 µs 改为 105 µs，
数据在 102 µs 就绪；重复等待、事件复用、尾部同步、身份缺失和顺序歧义检查通过。
当前代码在 R34 base 上恢复 41496 个逻辑等待点，另有 112800 个 native 调用已有设备记录；
HTTP 仍为 31.599557 秒，phase 同成本回放不改变任何变换前节点的完成时刻，证据 r38_source_same_cost_verified.json。
50 项 Python 测试、C++ 检查和两种构建通过。尚未完成 HiCache 逐层调用的增删，也没有新 cross 精度；
下一步同 base 单次补采已经启动，用于取得 R37 的精确计数器边界，不扩采 target 或矩阵。

R39 的同 base 补采已完成，目录为 `data/profile_runs/sglang/20260916_000201_e2e_layer_waits_C5_W4_TP4`。
正式 HTTP 窗口为 31.604075 秒，32 个请求的 forced-token 输出全部匹配，4 份 Torch、4 份 native 和 5 份 Python trace 齐全。
原命令在导出后写 manifest 时退出码为 1：入口 `profile.py` 遮住了同名标准库，导致 Torch 导入失败。
入口已修复；从已有产物恢复了 manifest，没有重跑负载，原失败及恢复时间口径保存在 `manifest_recovery` 中。

`r39_layer_wait_ownership.json` 的 432 个 batch 中，41472 次已记录等待均能找到唯一提交和 CPU worker，
微秒与纳秒检查都没有切到 CPU 叶子边界，区间未归属时间为 0。这仅证明已记录调用的 CPU 边界，
不代表设备依赖已全部核实，更不代表跨配置预测已通过。
另一个未完成项是 consumer 未启用时的调用观测：框架仍调用 `wait_until`，现探针却跳过了这些快速返回区间。
后续需补齐这些调用位置，再实现逐层等待的双向增删；本次整理不新增预测结果或改动成本参数。

本次提交前再次通过 51 项 Python 测试、ruff、C++ I/O 和时序检查，以及 validation / Release 增量构建检查。
代码按采集层、建图层和文档层提交，测试随对应实现保留；原始数据及 `docs/tmp` 计划日志仍保留在本地，不纳入 Git。

R40 修复了一处原生 trace 参数错配：wrapper 内的准备/调度间隔很长时，按开始时间最近会拿走下一条调用的参数，
引起 54 条连续错位，进而令一个 rank 的 stream 归属冲突。现按调用区间距离关联，等距候选不猜测。
新 base 的 41472 次活动等待现已全部接到设备等待与 Record，CPU 边界无切叶、无未覆盖时间。
原图和 phase 同成本的 HTTP 均为 31.604299 秒，5114575 个变换前节点时刻完全不变；
相对本次 HTTP 实测差约 0.224 ms。证据为 r40_wait_dependencies_repaired.json、r40_source_same_cost.json。

未启用 consumer 的短调用观测也已补齐代码与小测试，但尚未以新探针重采模型。
CPU 通信另有一个已确认的身份问题：monitored_barrier 会让各 rank 的底层序号推进不同，不能直接跨 rank 配对。
实际四进程 Gloo 测试复现了该机制；后续改用有完整覆盖证据的共同通信调用顺序，不能硬编码 rank 偏移。
51 项 Python 检查、C++ 检查与两种构建通过。逐层调用的双向变换及完整 cross 仍未完成，本轮没有新预测精度。

R41 已为 CPU 通信探针增加按进程组共享的观测调用序号，同时保留底层序号与首次观测位置。
实际四进程 Gloo 测试通过自动启动入口和 NPU 兼容包装：各 rank 的观测顺序一致，
即使 barrier 后底层序号分别为 7、3、3、3，也不再将它们误当成相同通信必须一致的身份字段。
证据为 r41_cpu_collective_runtime.json；这验证的是采集字段，不代表跨 rank 通信依赖已接入模型。
本次整理提交前重新通过 53 项 Python 测试、ruff、C++ I/O 与时序检查、validation / Release 增量构建检查。
采集代码、建图修复及文档分层提交，测试随实现保留；数据资产与临时计划日志仍仅保存在本地。

## 09-14 泛化结果

新 workload / 新 HiCache 配置 / TP=4 初步泛化实验已完成：C5 base × W4/W5 × G1/G2/G3，
TP=2→TP=2 与 TP=4→TP=4 各 6 格，合计 12/12 预测、target 采集和独立评分齐全。
不是跨 TP 扩图；新 workload 的 base profile 提供 source DAG/工作量，但不参与参数拟合。

| 当前新面板 | TP=2 六格 | TP=4 六格 | 总体十二格 |
| --- | ---: | ---: | ---: |
| 组合 scope WAPE | 0.276% | 0.389% | 0.317% |
| Scope p90 / 最大 APE | 0.935% / 0.935% | 1.453% / 1.453% | 0.935% / 1.453% |
| HiCache I/O/控制 WAPE | 12.950% | 16.209% | 14.772% |
| 不抵消 I/O 分项 WAPE | 17.463% | 20.486% | 19.153% |
| HiCache / phase 结构严格一致 | 4/6；5/6 | 4/6；5/6 | 8/12；10/12 |

组合 scope 主目标通过，但排除了 residual gap 和未归属成本，不是完整墙钟 E2E；最终仍为 `MODEL_LIMITATION`。
Prefill compute 总体 WAPE/p90 为 1.662%/8.634%，Decode 为 0.151%/0.476%；Prefill 与 I/O 分项仍未达标。
G2/W4 在两种 TP 均暴露完整前缀匹配边界缺陷（预测 Prefill 0，实际每 rank 128 tokens）；
G3 四格的等待/可见性投影尚未对齐，不能把严格结构失败直接说成预测 DAG 漏了等待。
这两项新问题尚未修复；未因新 target 成绩调参或追加校准，保留原始外推成绩。

本轮完成首次正式分配写回的归属修复、可选 NPU profiler 串行导出、TP4/W5 source 原始数据重导出恢复，
并通过机制测试、代表格与十二格正式原生回归。全部十九份普通 profile 请求成功，最终十二份 target 的
独立 phase/I/O 观测 ready，无无效事实、归属冲突或 token 范围错误。早期失败记录仍保留。
正式结果：`data/modeling_runs/hicache_generalization_20260914/generalization_summary.json`。
详细配置、逐 workload/单格结果、参数边界及后续问题见 [主验证文档第 10 节](validation/hicache_validation.md#10-09-14-新-workload--新配置--tp4-泛化结果)，
过程见 [泛化计划](tmp/hicache_generalization_plan_20260914.md) 与 [泛化日志](tmp/hicache_generalization_log_20260914.md)。

以下为 09-12 历史对照结果；没有在本轮首次分配归属修复后重跑旧 60-cross，不与新面板混算。

本阶段完成验证合同与控制边界修复、新 60-cross、独立评分与五格真实成本回放。
普通预测没有调参、补采或缩小评分范围；主要变化是消除旧验证工具造成的错误归因。
见 [本轮计划](tmp/hicache_gap_excluded_causal_plan_20260912.md) 与 [执行日志](tmp/hicache_gap_excluded_causal_log_20260912.md)。

## 1. 09-12 对照状态

“一个固定小型校准 + 一个 base 的 profiles，预测全部 HiCache target”主流程已经完成逻辑修复并执行新的
5-base/60-cross 验证。软件流程和信息边界可用，但严格模型验收没有全部通过：

- 60/60 预测 READY，60/60 位于当前固定校准记录的 I/O 域；
- 60/60 effect shape 与 phase 结构严格一致；原 40 格差异是 target 漏字段造成；
- 排除 residual CPU gap 的组合 scope：WAPE 0.921%、p90 1.981%，总体及五个 base 均通过；最大单格 APE 2.712%；
- Prefill+Decode combined：WAPE 0.694%、p90 2.316%，通过；phase delta 1.383%，54 个大变化方向全对；
- Prefill 单项 p90 3.436%，略高于 3% 门槛；
- HiCache I/O/控制：WAPE 5.186%、p90 32.518%、delta 8.121%；不抵消分项 WAPE 5.995%，失败；
- 最终状态为 `MODEL_LIMITATION`，不是工作流错误，也不是全 gate PASS。

09-12 对照正式结果：

```text
data/modeling_runs/hicache_gap_excluded_causal_20260912/final_60_evaluation/gate_summary.json
```

## 2. 本轮逻辑修复

### 本次修复

- 成本回放不再清零正载荷 Prefetch 的 terminal control；删除过时的 outcome-only 分类，保留真实 CPU 工作。
- 控制节点由操作是否存在决定，不因 duration 为零而消失；避免后台 service 意外成为前台前驱。
- target shape 从同一 target trace 的 Prefetch I/O 完成记录传播 visibility 页数，不用默认 0 冒充观测。
- oracle 先检查相同成本回放恒等性，再比较 target 成本；异常保留具体失败字段，不仅输出 READY。

### 保持的模型边界

- 已执行 I/O 与缓存可见收益分开；状态推进与 DAG 共用 service cost，timeout 等待与后台传输分开。
- DMA 使用设备时钟，存储使用函数墙钟代理，后者可能含内部调度等待。
- 三组件组合 scope 保留必要资源/消费者依赖，排除 residual gap 和未归入组件的 wrapper/probe 成本；不是完整墙钟只减 CPU 空白。
- 分项总量、非抵消误差、适用域和环境信息完整保留，不能通过跨组件抵消或隐式倍率过关。

## 3. 当前正式流程

```text
一个 base 的 3 个 profiles
        +
共享平台物理校准
        +
共享固定小型校准（page 32/128，各 2 次）
        ↓
观测提取 → 简洁成本模型 → source DAG 状态/工作量变换
        ↓
Prefill/Decode 变换 → 原子 DAG patch → 模拟
        ↓
全部预测完成后，独立打开 target profiles 评分
```

target config 在预测时决定预取、写回、容量等行为；target trace 和 target cost 只在最后评分/显式 oracle 中使用，
不会回写模型。当前五个 model build summary 的 `target_inputs`、`target_score_inputs` 都为空。

## 4. 验证证据

- Release 和 validation 全量 C++ 构建通过；`hicache_io_logic_check` 覆盖零/正控制耗时、best-effort/timeout 与 oracle 控制保留。
- 15 项 Python 语义回归和 ruff 通过。
- 固定校准两端点各 2 次成功重复，四类 service 的相对范围均小于 4.3%。
- 5 个 base 各 12 个 cross 全部 READY；60 个 cell 都在固定校准记录的域内。
- 每个 base 选择 1 个 cell，执行 1 次相同成本与 5 次真实成本组合；30 次全部通过，五格恒等性全部成立。
- evaluator 新提取 15 个 target DAG，只在全部预测完成后打开，`parameters_or_capture_plan_changed=false`。
- `current_result_audit.json` 逐值核对 60 格 source manifest 和完整 C++ 模型输入不变；36 格只增加零成本控制边界，耗时不变。

旧 Oracle 的 READY 不能证明成本替换有效：它没有检查相同成本回放恒等性，且会清零 Prefetch 控制成本并改变依赖。
旧约 13% 偏差因此撤回为真实调度误差证据。修复后 C5/W1→C4 完整成本回放误差为 0.0059%；
五格中四格低于 0.01%，C4/W2→C3 仍为 1.272%，保留为尚未单独归因的时序投影残差。
effect shape 与工作量比较正确，仍不等于完整 target 时序同构。

当前 5×3 矩阵没有触发 required/partial capacity gate；这一路径只有机制级证据，尚无本矩阵数据级覆盖。

## 5. 数据资产

当前修复运行根目录：

```text
data/modeling_runs/hicache_gap_excluded_causal_20260912/
```

其中包含：

- 五个 `C*/predictions/`：当前 60 个预测及复现输入；
- `representative/`、`representative_oracle/`：机制代表格和显式成本回放；
- `final_60_evaluation/`：15 个新 target DAG 观测、60 个评分 cell 与正式汇总；
- `identity*/`、`shape*/`、`current_result_audit.json`：根因反例与修复对照。

模型没有重拟合：`data/modeling_runs/hicache_io_logic_repair_20260911/` 中的物理校准、五个 base 的模型、
model build summary、base observations、targets 及其引用的原始 5×3 profiles/固定校准仍是当前必需输入，不能删除。
09-11 预测和更早结果保留为对照，不代替当前验收。

## 6. 结果应如何理解

现在可以正式陈述：

1. 一个固定小型校准加一个 base 的 profiles 能构建并运行所有当前 target 预测；
2. 实际执行 I/O、缓存可见收益、前台等待和后台资源占用已经从概念与实现上分开；
3. 配置决定的 HiCache 工作量/关系在当前矩阵没有不变量差异；
4. 排除 residual gap 的组合 scope 达到本阶段精度要求，但 HiCache I/O/控制分项未达到旧严格门槛；旧调度差异归因已撤回；
5. 当前数字不能称为完整请求 E2E，也不能声称“DAG 完全正确”。

## 7. 09-12 停止边界与当时的后续方向

本轮在看过最终 60-cell 结果后没有修改公式、系数或采集计划，也没有恢复逐 target 补采和 residual correction。
总体及逐 base 组合 scope 目标已通过；HiCache I/O/控制及部分 phase 分项限制保留，不因分项偏差追加经验修正。

当时提出单独研究 target-independent、可复现的调度/资源机制，重点是 best-effort 后台 I/O 如何影响 source
骨架中的资源时序，以及存储函数墙钟能否取得更明确的分解；当时 residual CPU gap 仍 deferred。09-15 的进一步诊断和当前优先级见文首。KTransformers 和 NodeScale
均保留：KTransformers 作为另一目标推理框架，NodeScale 作为默认关闭的可选 what-if，不进入默认 SGLang HiCache 流程。
